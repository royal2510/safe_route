version: '3.8'
services:
  app:
    build: .
    container_name: safe-route-ai
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGO_URI=mongodb://mongo:27017/saferoute
      - JWT_SECRET=safe-route-secret
      - NODE_ENV=production
    depends_on:
      - mongo
  mongo:
    image: mongo:7
    container_name: safe-route-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:
